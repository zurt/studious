{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "make test 2>&1 || true",
            "timeout": 120,
            "statusMessage": "Running tests after commit..."
          }
        ]
      }
    ]
  }
}
